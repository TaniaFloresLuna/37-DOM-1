function validateForm(){
	/* Escribe tú código aquí */

	//Función de contraseña
	function comproPass{
		var largo = "No";
		var contrasena = document.getElemntById(input-password).clave
		if(contasena.value.){

		}
	}
}